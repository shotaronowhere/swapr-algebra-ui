#name: Deploy
#
#on:
#  push:
#    branches:
#      - dev-deploy
#
#jobs:
#  deploy:
#    runs-on: ubuntu-latest
#    strategy:
#      matrix:
#        node-version: [12.x]
#    steps:
#      - uses: actions/checkout@v2
#      - uses: actions/setup-node@v1
#        with:
#          node-version: ${{ matrix.node-version }}
#      - uses: borales/actions-yarn@v2.3.0
#        with:
#          cmd: install
#      #setup key
#      - run: set -eu
#      - run: mkdir "$HOME/.ssh"
#      - run: echo "${{ secrets.SHHKEYM }}" > "$HOME/.ssh/SHHKEYM"
#      - run: chmod 600 "$HOME/.ssh/SHHKEYM"
#      #build
#      - run: yarn build
#      #deploy
#      - run: cd build && rsync -e "ssh -i $HOME/.ssh/SHHKEYM -o StrictHostKeyChecking=no" --compress sftp://frontend@3.143.136.254/var/www/dev-algebra-finance